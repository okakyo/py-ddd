import React from "react"
import { Box } from '@chakra-ui/react'

type BaseCardProps = {
  title: string

}
export const BaseCard = (props: BaseCardProps) => {
  const { title } = props
  return (
    <Box>
      <Box>
        
      </Box>
    </Box>
  )
}
